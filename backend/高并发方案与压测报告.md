# 天水流域生态环境监管平台：高并发方案与压测报告

## 1. 交付范围与结论

本次改造对象是“遥感影像生态指数计算”的任务提交链路，不把 HTTP 受理性能误写成 GeoTIFF 栅格计算性能。接口现在只创建持久化任务并尝试投递，返回 `202 Accepted`；Rasterio/GDAL/RSEI 的重计算由 Celery Worker 独立执行。

已完成一次真实 HTTP 压测：临时压测账号先通过真实 Session/CSRF 登录，再通过本机 Nginx 向 Django 接口发送 300 次、32 并发、同一 `X-Idempotency-Key` 的请求。300 次均返回 `202`，PostgreSQL 实际只创建 1 条 `ProcessingTask`，响应中只出现 1 个任务 ID，压测判定通过。

最终机器可读结果保存在 `backend/benchmark_submission_postgresql_300x32_pass.json`，由仓库内的 `benchmark_concurrency` 管理命令生成。

## 2. 实际架构

```text
Vue（每次业务提交带 X-Idempotency-Key）
            |
            v
Nginx（提交入口限流/连接保护；地图瓦片、静态和媒体不受该限流）
            |
            v
Django + DRF
  ├─ ProcessingTask：幂等键、请求指纹、活跃指纹、优先级、持久化 outbox 状态
  ├─ 数据库 UNIQUE(active_fingerprint)：跨 Web 实例的 single-flight 仲裁
  └─ 202 返回
            |
            v
Redis Broker / Celery 队列（geo.high / geo.default / geo.low）
            |
            v
Celery Worker（Linux prefork；Windows threads） -> Rasterio / GDAL / GeoServer
```

### 幂等与跨实例去重

- `X-Idempotency-Key` 合法长度为 8–128。相同键、相同影像/指数/优先级返回原任务；相同键但参数不同返回 HTTP `409`。
- `request_fingerprint` 包含影像、规范化（排序去重）指数集合、用户和优先级，用于判定同键参数冲突。
- `active_fingerprint` 只包含同一业务计算维度（影像、指数集合、用户），并由数据库唯一约束保证同时只能有一条活跃任务。该约束才是多 Web 实例下的最终裁决，不依赖进程内锁。
- 任务完成、失败或取消时清空 `active_fingerprint`；取消接口为 `POST /api/v1/environment/processing-tasks/{id}/cancel/`。实测取消后可用新幂等键创建新的任务。

### 可靠投递与任务状态

- 创建任务先落库：记录 `celery_task_id`、`queue_name`、`priority`、`dispatch_status`、`dispatch_attempts`、`dispatching_at` 和 `last_dispatch_error`。
- 投递状态是持久化 outbox：`pending -> dispatching -> dispatched/failed`。投递失败不删除任务；`python manage.py dispatch_pending_tasks --limit 100` 会补投 `pending`、`failed`，以及超时未完成认领的 `dispatching` 记录。
- Celery 使用确定性的 task id（等于 `ProcessingTask.id`），并启用持久消息、`acks_late`、worker 丢失退回队列、`prefetch=1`、25 分钟软超时、30 分钟硬超时。其语义是 **at-least-once**，因此 GIS 任务必须保持可重入，不能假设绝对的 exactly-once 消费。
- 优先级：`high -> geo.high (9)`、`normal -> geo.default (5)`、`low -> geo.low (1)`。

## 3. 部署参数

### Django / PostgreSQL

生产配置 `tianshuipy.settings_postgresql` 已支持：`CONN_MAX_AGE`（默认 300 秒）、`CONN_HEALTH_CHECKS=True`、`DB_CONNECT_TIMEOUT`（默认 5 秒）。生产库应使用 PostgreSQL（GIS 数据需要空间查询时使用 PostGIS），并执行全部迁移。

### Celery

Windows 演示环境使用：

```powershell
python -m celery -A tianshuipy worker -l info --pool=threads --concurrency=4 --queues=geo.high,geo.default,geo.low -Ofair
```

Linux 生产环境使用 `prefork`，GIS Worker 的初始并发建议取物理 CPU 核数或其 1/2，并按可用内存下调；每个栅格计算进程可能占用数 GB 内存，不能只按 HTTP 并发数盲目放大：

```bash
celery -A tianshuipy worker -l info --pool=prefork --concurrency=<CPU核数或内存核定值> \
  --queues=geo.high,geo.default,geo.low -Ofair
```

### Nginx

- `worker_processes auto`、`worker_connections 4096`、后端 upstream keepalive 64。
- 仅生态指数提交入口限流：每 IP 100 req/s，突发 200，提交连接上限 50；一般 API 仅连接保护 100。
- `/media/`、Vue 静态资源、GeoServer OWS 与 WMS/WFS API 代理不会被提交限流误伤。
- `client_max_body_size 10g`；上传/长请求的连接、发送、读取超时均为 10 分钟。

## 4. 真实压测数据

### 环境

| 项目 | 实际值 |
| --- | --- |
| 日期 | 2026-09-18 |
| 操作系统 | Windows 本地开发机 |
| 主机资源 | Intel Core i7-13700KF（24 逻辑处理器）、128 GB 内存；C: NVMe、D: SATA HDD |
| Python 环境 | Conda `ts`，Python 3.10.21 |
| Web / 数据库 | Django 5.2.4、PostgreSQL 16.11 + PostGIS 3.5（单 Web 实例） |
| 访问路径 | `http://127.0.0.1:8081`，经本机 Nginx 反向代理 |
| 异步设置 | Redis 8.10.1 服务，`CELERY_TASK_ALWAYS_EAGER=false`；压测期间暂停 GIS Worker，避免占位影像进入计算 |
| 认证路径 | 临时账号经 `/api/v1/users/login/` 获取真实 Session/CSRF，再发送并发写请求 |
| 统计范围 | 任务提交/数据库去重控制面；未执行 GeoTIFF 计算 |

### 结果

| 指标 | 实测值 |
| --- | ---: |
| 总请求 / 并发 | 300 / 32 |
| HTTP 202 成功率 | 100%（300 / 300） |
| 吞吐 | 155.61 RPS |
| 平均延迟 | 183.99 ms |
| P50 / P95 / P99 | 96.30 / 990.39 / 1723.91 ms |
| 最大延迟 | 1907.64 ms |
| 数据库实际创建任务数 | 1 |
| 返回的不同任务 ID 数 | 1 |
| 判定 | 通过 |

另外通过真实 HTTP 验证：同一幂等键但指数不同返回 `409`；取消原任务返回 `200` 后，以新幂等键提交相同影像/指数返回 `202` 且获得新的任务 ID，证明活跃锁已释放。

## 5. 复现命令

先启动 Web（压测中不启动 GIS Worker，避免将不存在的占位影像投入计算）：

```powershell
conda activate ts
cd D:\Zhangyong\Agents\tianshui_projectSU\backend
$env:CELERY_TASK_ALWAYS_EAGER='false'
$env:DJANGO_SETTINGS_MODULE='tianshuipy.settings_postgresql'
$env:CELERY_BROKER_URL=[Environment]::GetEnvironmentVariable('TIANSHUI_CELERY_BROKER_URL', 'User')
$env:CELERY_RESULT_BACKEND=[Environment]::GetEnvironmentVariable('TIANSHUI_CELERY_RESULT_BACKEND', 'User')
# 仅本机 HTTP 压测使用；正式 HTTPS 网关保持 SECURE_SSL_REDIRECT=true。
$env:SECURE_SSL_REDIRECT='false'
$env:SESSION_COOKIE_SECURE='false'
$env:CSRF_COOKIE_SECURE='false'
python manage.py runserver 127.0.0.1:8000 --noreload
```

确保 Nginx 已加载 `deployment/nginx.conf`，另开终端执行：

```powershell
conda activate ts
cd D:\Zhangyong\Agents\tianshui_projectSU\backend
python manage.py benchmark_concurrency --base-url http://127.0.0.1:8081 --requests 300 --concurrency 32 --authenticated --report-file .\benchmark_submission_postgresql_300x32_pass.json
```

压测命令使用真实 HTTP、共享幂等键，并自动创建占位影像、任务与临时压测账号。`--authenticated` 会通过实际登录接口获取 Session/CSRF；它检查 202 数、数据库创建数及返回任务 ID 数，任一项不符合“300 / 1 / 1”即以非零状态退出。通过后任务会被标记为已取消并释放活跃锁；Worker 消费到该消息时会在 GIS 计算前安全忽略，避免为清理测试数据而制造 Broker 孤儿消息。

Broker 短暂故障后的补投：

```powershell
python manage.py dispatch_pending_tasks --limit 100 --stale-seconds 300
```

## 6. 风险边界与下一步

本报告的 155.61 RPS 是 **任务提交控制面** 指标，绝不是“每秒完成 155.61 景 GeoTIFF/RSEI”。本次实测已覆盖 PostgreSQL、Redis、Nginx、真实认证写请求和单 Web 实例，但尚未覆盖多 Web 实例、Linux prefork Worker 或栅格计算本身。

要开展端到端栅格压测，需提供：

1. 可合法用于测试的真实 GeoTIFF 样本及预期输出口径；RSEI 至少需要已校准的可见光/NIR/SWIR 与可靠热红外/LST 数据，建议按小、中、大三种文件量级准备多景样本。
2. Linux 目标机或容器规格（CPU、内存、磁盘类型/剩余空间），以及 PostgreSQL/PostGIS 与 Redis 的连接信息或部署权限。
3. GeoServer 的数据目录/工作区权限和性能监控口径（队列等待、单景耗时、成功率、内存峰值、I/O、发布耗时）。

拿到这些后应单独压测：队列等待时间、每景端到端完成时间、并行 Worker 数下的完成率与资源曲线，并用实际完成景数/小时或景数/天作为“栅格计算吞吐”指标。

## 7. 可用于简历的表述

> 负责天水流域生态遥感平台任务提交链路并发改造：基于 Django/DRF、PostgreSQL 唯一约束和 Celery 持久化 outbox 实现幂等提交、跨实例 single-flight 去重、优先级队列与失败补投；在 Redis + PostgreSQL 环境下经 Nginx 的真实认证 HTTP 压测中，32 并发 300 次相同请求实现 100% 受理，任务提交控制面吞吐 155.61 RPS、P95 990.39 ms，数据库仅创建并返回 1 个 GIS 任务。

该表述仅对应任务提交控制面实测，不包含 GeoTIFF/RSEI 的端到端计算吞吐。

## 8. 端到端 GIS 处理压测（真实 Landsat 9）

### 数据与链路

数据为真实 Landsat 9 Collection 2 Level-2 产品 `LC09_L2SP_233248_20250726_20250728_02_T2`。基准输入由 SR_B2--SR_B7 与 ST_B10 按窗口合成为 7 波段 GeoTIFF；大小 449,911,512 bytes，8111×8051 像元、30m、EPSG:32629、Nodata=0。反射率使用官方 scale=0.0000275、offset=-0.2，地表温度使用 scale=0.00341802、offset=149K。链路覆盖 Nginx → Django API（真实 Session/CSRF）→ PostgreSQL → Redis → Celery threads Worker → Rasterio/GDAL → GeoTIFF 结果与 EcologicalIndex 落库。

### 已完成实测

| 场景 | Worker 并发 | 成功 | 端到端 / 执行 | 吞吐 | 峰值 RSS | 结果文件 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| NDVI，单景 | 1 | 1/1 | 13.57s / 13.40s | 4.225 tasks/min | 4.17GB | 261.3MB |
| RSEI 分量联合（greenness/wetness/dryness/heat + RSEI），单景 | 1 | 1/1 | 60.85s / 60.71s | 0.973 tasks/min | 11.86GB | 1.306GB（5 个 GeoTIFF） |

原始记录：`benchmark_gis_e2e_ndvi_c1_t1.json/.csv`、`benchmark_gis_e2e_rsei_c1_t1.json/.csv`。RSEI 文件与 EcologicalIndex 已落库；附表 RSEIResult 的 NumPy 数组判定缺陷已修复，后续并发轮次将加载修复后的 Worker。

### 容量边界（当前结论）

单景 RSEI 峰值约 11.86GB，因此在 128GB 主机上不应直接将 8 个完整 RSEI 同时交给 threads Worker：仅栅格数组即可能接近 95GB，还未计入操作系统、数据库缓存和结果写盘。容量阶梯实测与最终建议见下一节。

## 9. 容量阶梯压测与推荐配置

### 方法与口径

所有档位均使用同一份真实 449.9MB Landsat 9 七波段输入；每个任务创建独立 `RemoteSensingImage` 数据库记录，因此其任务指纹不同，不会被幂等 single-flight 合并。没有复制或伪造像元数据。每轮均经过 Nginx、真实登录后的 Django API、PostgreSQL 任务记录、Redis、Celery threads Worker、Rasterio/GDAL 计算、GeoTIFF 写盘及 EcologicalIndex 落库；测试结束后由报告枚举精确的临时记录/结果目录并清理，JSON/CSV 原始指标与基准输入保留。

时延分位数使用 nearest-rank：小样本中 P95/P99 取最慢任务，避免被向下取整误写成 P50。CPU/RSS 为 Celery Worker 主进程及其子进程合计峰值；系统可用内存取压测期最低值。磁盘为 D: SATA HDD，采集到的是已用空间变化，未采集 IOPS，因此只能据时延和输出写盘量提示 I/O 风险，不能将其写成已证明的 IOPS 瓶颈。

### 实测矩阵

| 指数 / Worker 并发 / 任务数 | 成功 | P50 / P95 / P99 / 最大端到端时延 | 吞吐（tasks/min） | CPU 峰值 | Worker RSS 峰值 | 最低可用内存 | Redis 队列峰值 | 输出 GeoTIFF |
| --- | ---: | --- | ---: | ---: | ---: | ---: | ---: | ---: |
| NDVI / 1 / 1（基线） | 1/1 | 13.57 / 13.57 / 13.57 / 13.57s | 4.225 | 103.0% | 4.17GB | 未采集 | 0 | 0.261GB |
| NDVI / 2 / 2 | 2/2 | 11.16 / 13.73 / 13.73 / 13.73s | 8.305 | 203.0% | 8.29GB | 91.63GB | 0 | 0.523GB |
| NDVI / 4 / 4 | 4/4 | 16.73 / 20.46 / 20.46 / 20.46s | **11.681** | 409.0% | 15.35GB | 84.25GB | 0 | 1.045GB |
| NDVI / 8 / 8 | 8/8 | 35.89 / 49.71 / 49.71 / 49.71s | 9.562 | 823.9% | 27.51GB | 72.54GB | 0 | 2.090GB |
| RSEI / 1 / 1（基线） | 1/1 | 60.85 / 60.85 / 60.85 / 60.85s | 0.973 | 509.7% | 11.86GB | 未采集 | 0 | 1.306GB |
| RSEI / 2 / 2 | 2/2 | 68.28 / 71.76 / 71.76 / 71.76s | 1.664 | 847.0% | 21.21GB | 79.03GB | 0 | 2.613GB |
| RSEI / 4 / 4 | 4/4 | 106.32 / 114.42 / 114.42 / 114.42s | **2.092** | 705.6% | 37.89GB | 62.40GB | 0 | 5.225GB |

容量阶梯的 20 个任务（NDVI 14、RSEI 6）均返回 HTTP 202、最终 completed；连同两个单景基线共 22 个任务均成功。没有 failed/cancelled、没有重试，任务投递次数均为 1。测试结束轮次没有 pending/processing 任务。数据库连接数在测试采样结束时为 39--49；Redis 队列峰值始终为 0，因 Worker 并发数等于提交任务数，未形成等待积压。

原始数据：

- [NDVI 并发 2 JSON](/D:/Zhangyong/Agents/tianshui_projectSU/backend/benchmark_gis_e2e_ndvi_c2_t2_monitored.json) / [CSV](/D:/Zhangyong/Agents/tianshui_projectSU/backend/benchmark_gis_e2e_ndvi_c2_t2_monitored.csv)
- [NDVI 并发 4 JSON](/D:/Zhangyong/Agents/tianshui_projectSU/backend/benchmark_gis_e2e_ndvi_c4_t4.json) / [CSV](/D:/Zhangyong/Agents/tianshui_projectSU/backend/benchmark_gis_e2e_ndvi_c4_t4.csv)
- [NDVI 并发 8 JSON](/D:/Zhangyong/Agents/tianshui_projectSU/backend/benchmark_gis_e2e_ndvi_c8_t8.json) / [CSV](/D:/Zhangyong/Agents/tianshui_projectSU/backend/benchmark_gis_e2e_ndvi_c8_t8.csv)
- [RSEI 并发 2 JSON](/D:/Zhangyong/Agents/tianshui_projectSU/backend/benchmark_gis_e2e_rsei_c2_t2.json) / [CSV](/D:/Zhangyong/Agents/tianshui_projectSU/backend/benchmark_gis_e2e_rsei_c2_t2.csv)
- [RSEI 并发 4 JSON](/D:/Zhangyong/Agents/tianshui_projectSU/backend/benchmark_gis_e2e_rsei_c4_t4.json) / [CSV](/D:/Zhangyong/Agents/tianshui_projectSU/backend/benchmark_gis_e2e_rsei_c4_t4.csv)

### 结论、瓶颈与配置

- **NDVI：推荐并发 4，已验证安全上限为 8。** 4 并发取得全矩阵最高吞吐 11.681 tasks/min；提升到 8 虽然未 OOM，但吞吐反降 18.1%，P50 时延由 16.73s 增至 35.89s。因此 8 仅能表述为“在本数据集上实测通过”，不应作为生产推荐值。
- **RSEI：交互/稳态推荐并发 2，批处理可采用并发 4；已验证安全上限为 4。** 4 并发全成功并将吞吐从 1.664 提到 2.092 tasks/min（+25.7%），但 P50 时延由 68.28s 升至 106.32s（+55.7%），RSS 从 21.21GB 增至 37.89GB。没有执行 RSEI 并发 8，故不能宣称其安全上限超过 4。
- **实测瓶颈判断：** Redis 没有积压、数据库连接稳定且无投递/计算失败，二者不是本轮限制因素；GeoServer 不在该计算任务的处理路径中，未参与本轮瓶颈判定。NDVI 8 并发时 CPU 仅约 8.24 个逻辑核而单景时延明显恶化，结合每景 261MB 的 HDD 结果落盘，说明瓶颈更可能是 GDAL/Rasterio 全幅数组、内存带宽与 SATA HDD 并发 I/O 的组合，尚需 PerfMon/IOPS 才能分离归因。RSEI 的 PCA 与五文件写盘使该问题更明显。
- **已实施队列隔离：** RSEI 分量集合（greenness/wetness/dryness/heat）现在自动路由到 `geo.heavy`；轻量 NDVI/NDWI/NDBI 保持 `geo.high`、`geo.default`、`geo.low` 优先级队列。Windows 当前建议单独运行轻量 Worker `--pool=threads --concurrency=4 --queues=geo.high,geo.default,geo.low -Ofair`，重型 Worker `--pool=threads --concurrency=2 --queues=geo.heavy -Ofair`。这两个 Worker **同时混跑**尚未压测，故是基于隔离目标的部署起点而非混合负载实测容量；Linux 应改为 prefork 并重新做相同矩阵验证。

### 复现与运维命令

```powershell
cd D:\Zhangyong\Agents\tianshui_projectSU\backend
# 轻量 NDVI：先启动只消费轻量队列的 Worker，再以 4 个独立任务压测
python -m celery -A tianshuipy worker -l info --pool=threads --concurrency=4 --queues=geo.high,geo.default,geo.low -Ofair
python manage.py benchmark_gis_e2e --base-url http://127.0.0.1:8081 --media-path remote_sensing/benchmark/landsat9_c2l2_20250726_sr_b2_b7_st_b10.tif --indices ndvi --tasks 4 --submit-concurrency 4 --worker-concurrency 4 --worker-pid <PID> --report-file .\benchmark_gis_e2e_ndvi_c4_t4.json

# 重型 RSEI：独占重型队列，先以 2 并发作为生产起点
python -m celery -A tianshuipy worker -l info --pool=threads --concurrency=2 --queues=geo.heavy -Ofair
python manage.py benchmark_gis_e2e --base-url http://127.0.0.1:8081 --media-path remote_sensing/benchmark/landsat9_c2l2_20250726_sr_b2_b7_st_b10.tif --indices greenness wetness dryness heat --tasks 2 --submit-concurrency 2 --worker-concurrency 2 --worker-pid <PID> --report-file .\benchmark_gis_e2e_rsei_c2_t2.json

# 每批压测结束后，根据指定报告精确清理临时影像记录和结果文件；报告/CSV/输入影像不删除
python manage.py cleanup_gis_benchmark --report-file .\benchmark_gis_e2e_ndvi_c4_t4.json --confirm
```

### 可用于简历的表述（仅限实测范围）

> 完成天水流域生态环境监管平台真实 Landsat 9（8111×8051、7 波段、449.9MB）端到端 GIS 容量压测，链路覆盖 Nginx、Django、PostgreSQL/PostGIS、Redis/Celery 与 Rasterio/GDAL 结果落库；在 128GB Windows 主机上，NDVI 4 Worker 并发达到 11.681 tasks/min（4/4 成功），RSEI 联合计算 4 并发达到 2.092 tasks/min（4/4 成功，5.225GB 结果文件），据实测将轻量任务推荐并发设为 4、重型 RSEI 稳态并发设为 2 并隔离至独立队列。

其中 155.61 RPS 仍仅是任务提交控制面；NDVI/RSEI 的 tasks/min 才是本报告的真实栅格处理吞吐，二者不可互换。

## 10. GIS 性能优化 A/B 实验（真实 Landsat 9）

### 输入与可验证边界

用户指定的 `六波段　LST　NDBSI/stacked_image_6band.tif` 经 Rasterio 检查为 8051×8111、6 波段 uint16、783.7MB、无波段描述/scale/offset/nodata，目录内仅有 SR_B2--SR_B7；其中没有可验证的 LST/ST_B10，不能将第 6 个 SWIR 波段伪作热度参与 RSEI。因此本节在用户授权后使用同景、已校准的真实 7 波段栈 `landsat9_c2l2_20250726_sr_b2_b7_st_b10.tif`：SR_B2--SR_B7 + ST_B10，8111×8051、449.9MB、EPSG:32629，反射率和地表温度 scale/offset 完整。不存在合成像元或伪造 RSEI。

### 实施内容

- 任务编排保存 `calculated_arrays`，并将其传给 `calculate_rsei(components=...)`；默认不再第二次计算 greenness/wetness/dryness/heat。保留 `components=None` 的旧路径，仅用于基线 A/B 与兼容调用。
- 归一化差值、除法和归一化使用 `np.subtract`/`np.divide(..., out=..., where=...)`，避免额外整幅中间数组；nodata、NaN、Float32、PCA 公式和主成分符号校正未改。
- `calculate_statistics` 支持 512 行分块统计；PNG 仅为预览，最长边 2048 px，GeoTIFF 保持完整 8111×8051 分辨率。
- 输出使用 512×512 tiled、Float32、BigTIFF 安全选项，并由 `GIS_RASTER_COMPRESSION` 配置压缩：默认 `NONE` 为低延迟档，`DEFLATE`/`ZSTD` 为存储优先档（含 predictor=3）。分块和压缩不改变科学数组。

### 单景 A/B：实际阶段耗时

| 阶段 | 旧行为 | 快速优化（默认） | 变化 |
| --- | ---: | ---: | ---: |
| 影像读取 | 4.91s | 5.01s | +2.0% |
| 四分量计算 | 6.24s | 6.25s | 基本持平 |
| RSEI（含分量重复/PCA） | 14.12s | 9.38s | **-33.5%** |
| 统计 | 6.12s | 8.55s | +39.6%（以小块内存换时间） |
| 5 个 GeoTIFF 写入 | 4.58s | 2.85s | **-37.8%** |
| 5 张 PNG 预览 | 33.28s | 12.45s | **-62.6%** |
| 数据库落库 | 0.021s | 0.014s | - |
| 全流程 wall time | 52.99s | **34.53s** | **-34.8%** |
| 峰值 RSS | 10.92GB | **9.38GB** | **-14.1%** |
| GeoTIFF 总大小 | 1.306GB | 1.342GB | +2.7%（tile 开销） |
| 系统磁盘写入计数 | 1.21GB | 1.14GB | -5.8% |

存储优先 `DEFLATE` 档也完成真实 A/B：输出由 1.306GB 降至 **0.549GB**（-58.0%），系统写入由 1.21GB 降至 0.65GB；但 wall time 升至 66.12s（+24.8%）。因此 DEFLATE 不能写成计算加速，适合磁盘/网络容量优先的异步归档；当前 Windows SATA HDD 的默认生产档为无压缩 tiled 快速档。

### 数值与 GeoTIFF 完整性

`compare_rsei_ab` 对 legacy 与快速优化档的 5 个输出逐 tile 扫描了全部 65,301,661 个有效像元：greenness、wetness、dryness、heat、RSEI 的相关系数均为 1.0，MAE、RMSE、最大绝对误差均为 0；RSEI 五级面积差为 0 km²。CRS、transform、Float32 与 NaN nodata 相同。优化输出可由 Rasterio 正常读取，且为 tiled=True、512×512；快速档不压缩，DEFLATE 档 compression=deflate。以上验证满足严格容差 `1e-6`，允许优化路径作为默认实现。

### 并发复测与容量结论

| RSEI 场景 | 成功 | 吞吐 tasks/min | P50 / P95 端到端 | RSS 峰值 | 最低可用内存 | 输出总量 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| 优化前并发 2 | 2/2 | 1.664 | 68.28 / 71.76s | 21.21GB | 79.03GB | 2.613GB |
| 快速优化并发 2 | 2/2 | **2.629** | **44.01 / 45.33s** | **19.17GB** | 81.05GB | 2.684GB |
| 优化前并发 4 | 4/4 | 2.092 | 106.32 / 114.42s | 37.89GB | 62.40GB | 5.225GB |
| 快速优化并发 4 | 4/4 | 2.273 | 102.25 / 104.77s | 39.41GB | 60.96GB | 5.369GB |

快速优化并发 2 的吞吐提升 58.0%，P50 降低 35.5%，RSS 降低 9.6%，并且无失败/重试/Redis 积压。并发 4 虽通过并较旧版本吞吐提升 8.7%，但吞吐低于优化后的并发 2，RSS 更高；因此当前推荐 `geo.heavy` Worker 并发 **2**，并发 4 仅作为已验证可用的离线批处理档，不是推荐稳态配置。RSEI 并发 8 未执行。

### 分块 RSEI 评估

输出与统计已经分块，但完整 RSEI PCA 仍需要全局有效像元统计。简单对每块独立归一化/PCA 会改变 RSEI 的全局语义，不能替换默认算法。可行的后续方案是两遍扫描：第一遍取得每分量全局 min/max 和 IncrementalPCA 的全局统计，第二遍按窗口投影和写出；该路径会是近似 PCA，必须在固定随机样本与全栅格上验证相关性、MAE/RMSE、分级面积差后，才可以显式开关启用。本轮没有以近似算法替换默认精确 PCA。

### 原始结果与复现

- [旧行为基线 JSON](</D:/Zhangyong/Agents/tianshui_projectSU/backend/benchmark_rsei_ab_legacy.json>)、[快速优化 JSON](</D:/Zhangyong/Agents/tianshui_projectSU/backend/benchmark_rsei_ab_optimized_fast.json>)、[压缩档 JSON](</D:/Zhangyong/Agents/tianshui_projectSU/backend/benchmark_rsei_ab_optimized.json>)
- [快速档全像元一致性 JSON](</D:/Zhangyong/Agents/tianshui_projectSU/backend/benchmark_rsei_ab_fast_comparison.json>)
- [快速优化端到端并发 2 JSON](</D:/Zhangyong/Agents/tianshui_projectSU/backend/benchmark_gis_e2e_rsei_fast_c2_t2.json>)、[并发 4 JSON](</D:/Zhangyong/Agents/tianshui_projectSU/backend/benchmark_gis_e2e_rsei_fast_c4_t4.json>)

```powershell
cd D:\Zhangyong\Agents\tianshui_projectSU\backend
python manage.py benchmark_rsei_ab --media-path remote_sensing/benchmark/landsat9_c2l2_20250726_sr_b2_b7_st_b10.tif --mode legacy --report-file .\benchmark_rsei_ab_legacy.json --keep-artifacts
python manage.py benchmark_rsei_ab --media-path remote_sensing/benchmark/landsat9_c2l2_20250726_sr_b2_b7_st_b10.tif --mode optimized_fast --report-file .\benchmark_rsei_ab_optimized_fast.json --keep-artifacts
python manage.py compare_rsei_ab --legacy-report .\benchmark_rsei_ab_legacy.json --optimized-report .\benchmark_rsei_ab_optimized_fast.json --report-file .\benchmark_rsei_ab_fast_comparison.json --tolerance 1e-6
```

### 可用于简历的表述（严格基于实测）

> 优化 Landsat 9 真实 RSEI（8111×8051、65.3M 像元）处理链路：通过四分量复用、数组临时对象控制、分块统计、2048px 预览和 tiled GeoTIFF 写入，将单景全流程由 52.99s 降至 34.53s（-34.8%），峰值 RSS 由 10.92GB 降至 9.38GB（-14.1%）；5 个输出在全像元校验中相关系数 1.0、MAE/RMSE/最大误差均为 0。端到端 RSEI 并发 2 吞吐由 1.664 提升至 2.629 tasks/min，P50 由 68.28s 降至 44.01s。
